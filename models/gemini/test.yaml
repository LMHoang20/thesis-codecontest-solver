main():
  read n: 
  - read the number of elements in list a
  - n is an integer
  read a: 
  - read the elements of list a 
  - a is a list of integers
  result = (solve(a) + n) / 2:
  - calculate the result according to the formula
  solve(a):
  - calculate the sum of even numbers in a
    initialize s to 0
    iterate over a:
      if the element is even, add it to s
    return s
  print the result